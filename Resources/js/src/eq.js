// jquery plugin returning formula object with 
// needed equations for prediction methods.
EQ = function(opts){
  var defaults = {
  
  }
  if(!opts) opts = this.defaults;

  EQ.option_switch = function(){
  
  }

  EQ.sanchez = function(){
  
  }

  EQ.pythagorean = function(){
  
  }

  return EQ;
}
